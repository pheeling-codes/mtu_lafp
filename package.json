{
  "name": "mtu-lafp-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "pnpm db:setup && turbo run dev",
    "db:setup": "turbo run setup --filter=@mtu/database",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.2.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  },
  "packageManager": "pnpm@9.12.2",
  "dependencies": {
    "jszip": "^3.10.1"
  }
}
