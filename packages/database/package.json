{
  "name": "@mtu/database",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "generate": "prisma generate",
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "migrate:dev": "prisma migrate dev",
    "db:push": "prisma db push",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@mtu/shared-types": "workspace:*",
    "@prisma/client": "^5.10.0"
  },
  "devDependencies": {
    "@mtu/typescript-config": "workspace:*",
    "@types/node": "^25.6.0",
    "prisma": "^5.10.0",
    "typescript": "^5.6.0"
  }
}
